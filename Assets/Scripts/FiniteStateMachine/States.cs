using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum States {
    Idle,
    Move,
    Shoot
}

public static class StatesExtension {
    static void DoThing(this States state) {
        
    }
}
